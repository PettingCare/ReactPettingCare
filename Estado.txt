Solucionar Fetch con jwt token para arreglar los endpoints --> ARREGLADO

BACKEND:

·Endpoints:
	·get mascota{id}

·citas falta todo

FRONTEND:
	·RUTA (/), es el "Home" de nuestra pagina.
	·Fetch tabla que llame a get "/Mascotas/misMascotas/" con hiperlinks para ver una mascota en especifico "/Mascotas/misMascota{id}/"
	


Clinica falta el backend y el frontend y saber como redireccionar el inicio de sesion dependiendo del rol (propietario o Clinica):
	·formulario y endpoint crear centro
	·endpoint ver mi centro
	·endpoint ver las citas asociadas al centro